# Custom Face Recognition Dependencies
# Optimized for CPU-only deployment (AWS EC2 without GPU)

# DeepFace - Face recognition framework with multiple model support
deepface>=0.0.79

# TensorFlow - CPU-only version (optimized for EC2)
tensorflow-cpu  # CPU-only, no GPU dependencies

# Additional dependencies for face detection backends
# MTCNN (faster on CPU than RetinaFace)
mtcnn>=0.1.1

# Image processing
Pillow>=10.0.0

# Numerical operations
numpy>=1.24.0,<2.0.0

# OpenCV for faster image operations
opencv-python-headless>=4.8.0  # Headless version for servers

# Optional: For better CPU performance
# tf-keras>=2.13.0  # If needed for compatibility
